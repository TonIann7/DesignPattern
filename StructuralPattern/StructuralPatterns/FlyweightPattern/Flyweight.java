public interface Flyweight { //Interfaccia per gli oggetti flyweight 
    //contiene i metodi per impostare o recuperare lo stato condiviso
    public void doOperation(String extrinsicState);
    
}
